{
  "version": 2,
  "builds": [
    {
      "src": "client/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "build" }
    },
    {
      "src": "server/server.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["connections/MongoConn.js", "models/RegistrationSchema.js", "routes/registration.js", "utils/sanitize.js"]
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "server",
      "headers": {
        "Access-Control-Allow-Origin": "https://www.serranoartcamp.org"
      }
    },
    { "src": "/(.*)", "dest": "client" }
  ]
}